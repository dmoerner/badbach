[project]
name = "badbach"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mingus>=0.6.1",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
